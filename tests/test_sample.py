# Sample test file. It does not test anything meaningful in AAAPT.

import unittest

from AAAPT.utils import BufferTest


class Test_BufferSample(BufferTest):
    def testSample(self):
        self.assertTrue(False)


class Test_Sample(unittest.TestCase):
    def testSample(self):
        self.assertTrue(False)
